## This is a test script. I want to to test using the git checkout call incase I save something that I don't mean to.


#on line 4, push to the repo. On line 6, write somthing, then save it, but don't push. Try checkout to get back the pre-saved version.